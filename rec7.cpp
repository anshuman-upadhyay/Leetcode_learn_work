#include<bits/stdc++.h>
using namespace std;
bool isPalindrome(string s) {
    transform(s.begin(),s.end(),s.begin(), ::tolower);
    string p=s;
    i1=p.begin();
    i2=p.end();
    //  p.erase(remove(p.begin(),p.end(),' '),p.end());
    if(p==" "){
        return true;
    }
    else{
        while(i1<i2){
            
        }

            }        }
int main(){
    string s;
    cin>>s;
    isPalindrome(s);

}