#include<bits/stdc++.h>
using namespace std;
int main(){
    unordered_set<int>s;//in most of the the opertions have a time complexity of O(1) in worst case possible O(N)
    // just like a set where it only stores unique values but this one doesnt bother to even store the in a sorted manner 
    //every function that can be used in a set is usable execpt lower and upper bound


    return 0;
}